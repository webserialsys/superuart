services:
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: ["migrate"]
    env_file:
      - ./.env
    environment:
      RUN_MIGRATIONS: "1"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./src:/app/src
      - ./.env:/app/src/.env:ro
    networks:
      - data

  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: ["uv", "run", "--no-sync", "uvicorn", "src.app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    env_file:
      - ./.env
    environment:
      RUN_MIGRATIONS: "0"
    ports:
      - "8000:8000"
    depends_on:
      migrate:
        condition: service_completed_successfully
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "uv",
          "run",
          "--no-sync",
          "python",
          "-c",
          "import sys,urllib.request; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/api/v1/ready', timeout=3).status == 200 else 1)",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    volumes:
      - ./src:/app/src
      - ./.env:/app/src/.env:ro
    networks:
      - app
      - data

  worker:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: ["uv", "run", "--no-sync", "arq", "src.app.core.worker.settings.WorkerSettings"]
    env_file:
      - ./.env
    environment:
      RUN_MIGRATIONS: "0"
    depends_on:
      migrate:
        condition: service_completed_successfully
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "uv",
          "run",
          "--no-sync",
          "python",
          "-c",
          "from src.app.core.worker.settings import start_arq_service; start_arq_service(check=True)",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    volumes:
      - ./src:/app/src
      - ./.env:/app/src/.env:ro
    networks:
      - data

  db:
    image: postgres:16
    env_file:
      - ./.env
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - data

  redis:
    image: redis:alpine
    volumes:
      - redis-data:/data
    expose:
      - "6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      - data

  pytest:
    build:
      context: .
      dockerfile: Dockerfile
      target: test
    env_file:
      - ./.env
    depends_on:
      migrate:
        condition: service_completed_successfully
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: ["uv", "run", "--no-sync", "pytest", "tests"]
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
      - ./.env:/app/src/.env:ro
    networks:
      - data

volumes:
  postgres-data:
  redis-data:

networks:
  app:
  data:
